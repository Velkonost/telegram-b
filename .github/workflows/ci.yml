# GitHub Actions CI для telegram-b
# Назначение: Project repository added via interview pipeline
# Основной язык: unknown (специфичная конфигурация недоступна)

name: CI — telegram-b

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Info
        run: |
          echo "Репозиторий: telegram-b"
          echo "Основной язык: unknown"
          echo "Специфичная CI-конфигурация для этого языка отсутствует"
          echo "Требуется ручная настройка CI"